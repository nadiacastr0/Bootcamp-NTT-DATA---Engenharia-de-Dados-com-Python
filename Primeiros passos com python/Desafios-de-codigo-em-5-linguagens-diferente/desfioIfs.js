const valorSalario = parseFloat(gets());
const valorBeneficios = parseFloat(gets());

//Calcula o imposto através da função "calcularImposto"
const valorImposto = calcularImposto(valorSalario);
//Calcula e imprime a Saída 